<div class="jumbotron">
<h2>New Event</h2>

<%= bootstrap_form_for(@event) do |f| %>
    <%= f.text_field :title %>
	<%= f.text_area :description %>
	<%= f.date_field :date %>
    <%= f.submit "Submit",  class: "btn btn-large btn-primary" %>
<% end %>
</div>